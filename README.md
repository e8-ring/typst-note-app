# TODO

## 機能

### コア

- ストレージへのアクセス。ファイル操作。

### タグ機能

### リンク機能

概要 : 各メモに一意的な識別子を割り当てる。その値を取得できるようにする。メモ A の識別子値を所定の方法でメモ B 内に記述するとリンクが貼られ、メモ B からメモ A へ飛べるようにする。

その他アイデア :

- リンクをクリックしたときに、中身をその場で展開するように設定することもできるようにしたい。

## UI

- font size, line height, letter spacing など、Viewer 側のテキスト表示の設定をまとめたデータクラスを作り、設定できるようにする
- 左側サイドバー、エディタペイン、ビューアペインの領域の幅を変えられるように